# SPDX-FileCopyrightText: 2025-2026 Univention GmbH
# SPDX-License-Identifier: AGPL-3.0-only

include:
  - component: $CI_SERVER_FQDN/univention/dev/tooling/ci-components/buildkit@2
    inputs:
      job_name: "build_basedpyright_image"
      stage: build
      build_context: "."
      dockerfile_path: "docker/basedpyright-ucs/Dockerfile"
      image_name: "basedpyright-ucs"
      registry_image: "$CI_REGISTRY_IMAGE/basedpyright-ucs"
      build_args: |
        UCS_DEV_BRANCH=$UCS_DEV_BRANCH
      job_rules:
        - if: $UCS_DEV_BRANCH != ""
        - if: $CI_MERGE_REQUEST_ID
          changes:
            - docker/basedpyright-ucs/Dockerfile
            - requirements-typecheck.txt
        - if: $CI_COMMIT_REF_PROTECTED == "true"
        - if: $CI_COMMIT_TAG

variables:
  UCS_DEV_BRANCH:
    value: ""
    description: >-
      Optional: build basedpyright-ucs against this ucs repo dev branch slug
      (see docker/basedpyright-ucs/Dockerfile, ARG UCS_DEV_BRANCH) instead of
      the released errata channel. Leave empty for the normal, reproducible
      build.
