# Warning: This file is auto-generated and might be overwritten by
#          univention-config-registry.
#          Please edit the following file instead:
# Warnung: Diese Datei wurde automatisch generiert und kann durch
#          univention-config-registry überschrieben werden.
#          Bitte bearbeiten Sie an Stelle dessen die folgende Datei:
#
#       /etc/univention/templates/files/etc/cron.d/sfb-restore-management
#

# mail list of tagged windows hosts
@!@
if configRegistry.has_key('restoremanagement/cron') and not configRegistry['restoremanagement/cron'] == '':
        print '@%@restoremanagement/cron@%@ root /usr/sbin/sfb-rmm-list-tagged-hosts'
else:
        print '#* * * * * root /usr/sbin/sfb-rmm-list-tagged-hosts'
@!@

